const base = 0;
const above = 1;
const below = -1;

export const HEADER_Z_INDEX = base + above;
export const THUMBNAIL_Z_INDEX = base;

export const MODAL_Z_INDEX = base + above;
export const MENU_Z_INDEX = base + above;
