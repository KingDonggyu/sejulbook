export enum ColorVariant {
  INHERIT = 'inherit',
  PRIMARY = 'primary',
  ERROR = 'error',
  WARNING = 'warning',
}

export enum ButtonVariant {
  TEXT = 'text',
  CONTAINED = 'contained',
  OUTLINED = 'outlined',
}

export enum ScreenModeState {
  DARK = 'Y',
  LIGHT = 'N',
}
